# Azure Functions
azure-functions>=1.23.0

# Basic dependencies for now
requests>=2.28.0

# Hugging Face Transformers (tokenizer + model)
transformers>=4.41.0
tokenizers>=0.15.2
sentencepiece>=0.1.99

# Optional: needed for generation with AutoModel* (CPU ok)
# If deployment environment manages torch separately, adjust/remove as needed.
torch>=2.2.0