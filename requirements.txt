# Azure Functions
azure-functions>=1.23.0

# Basic dependencies for now
requests>=2.28.0

# Hugging Face Transformers (tokenizer + model)
transformers>=4.41.0
tokenizers>=0.15.2
sentencepiece>=0.1.99

# Optional for generation with AutoModel*: torch is heavy on Azure Consumption plan
# To enable generation, deploy on a plan with sufficient resources and add torch accordingly.